#!/bin/sh
set -e

if [ "$1" = "remove" ] || [ "$1" = "purge" ]; then
    # Clean up byte-compiled Python files
    rm -f /usr/share/statsite/sinks/*.pyc
    rm -f /usr/share/statsite/sinks/__pycache__/*.pyc
fi

#DEBHELPER#

exit 0